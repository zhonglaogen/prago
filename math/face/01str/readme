字符串反转 三步反转
字符串包含(排序和ascii映射)
字符串转为整数 遍历*10 + arr[i]
回文判断(两边向中间靠拢,中间向两边扩散)
最长回文子串(枚举所有子串,找到中间字符扩展,添加#变成奇数字符串根据对称性质)
要求一个序列中最小的k个数(排序,堆排序,快排思想)
寻找和为定值的两个数(hash求差是否存在,)
寻找和为定值的多个数(递归,背包问题)
最大连续子数组和(三层for循环,如果加上第i个小于前面的和,就重第i个开始算起)
一个文件中含有n个元素（n未知），要求在只能遍历一遍这n个元素的情况下，等概率随机的取出其中之一个元素。,随机取出一个元素