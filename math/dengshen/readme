p1 
1.两个字符串,字母相同,顺序不同(acicc码映射)
2.数组内的数字中有个数的相同的数量大于数组的一半，求这个数字(栈,或者临时变量去记录)

p2
1.查找丢失的数字，第一种方法用一个额外的数组
2.查找丢失的数字，第二种，求和公式，然后减掉所有的值就是丢失的值
3.与数组下标异或,或者是与len取余,通过/和%
4. 所有的数字都是两个，只有一个数字是一个，找出这个数字(异或)

p3
1.判断是否是2的n次幂(n%2为0,就/2,直到最后为1)
2.(n-1) & n == 0
3 n & -n == n
4 1<<30 /%n ==0

p5
1.求二进制数字1的个数(1 & n == 1 count++ n=(n>>1))
2.求二进制数字1的个数(归并的思想,先算两个相邻的1个数,再算四个,最后算16个)
3.倒置二进制数字(先整体在局部,先倒置前8和后8位,在倒置4位)((num & m4) << 4) + ((num >> 4) & m4)